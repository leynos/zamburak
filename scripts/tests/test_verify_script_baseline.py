"""Matching-test stub for ``scripts/verify_script_baseline.py``.

This file satisfies the script-baseline checker's matching-test contract.
The actual tests are split across focused modules:

- ``test_verify_script_baseline_cli.py`` — CLI entrypoint tests
- ``test_verify_script_baseline_validation.py`` — validation rule tests

Usage
-----
Run with:

```
make script-test
```
"""
